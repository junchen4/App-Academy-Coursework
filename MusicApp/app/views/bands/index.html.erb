<h1> Here are the bands: </h1>

<ul>
  <% @bands.each do |band| %>
    <li> <a href="<%= band_url(band) %>"> <%= band.name %>  </a> </li>
  <% end %>
</ul>

<a href="<%=new_band_url%>"> Create a Band </a>
